# Ubuntu FaceAuth — Phase 1 dependencies
# Install inside the 'face-detection' conda environment:
#   pip install -r requirements.txt

# Core computer vision
opencv-python>=5.0.0

# Face detection + ArcFace recognition
insightface==0.7.3

# ONNX model runtime (CPU)
# 1.19+ required — compiled against numpy 2.x
onnxruntime>=1.19.0

# Numerical computing
numpy>=2.0.0

# Image I/O (used internally by insightface)
pillow>=10.0.0

# ML utilities (pulled in by insightface)
scikit-learn>=1.3.0
scikit-image>=0.21.0
scipy>=1.11.0

# Version comparison (used by scripts/check_env.py)
packaging>=23.0

# Testing
pytest>=7.0.0
